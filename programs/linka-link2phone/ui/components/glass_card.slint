export component GlassCard inherits Rectangle {
    in-out property <color> card-bg: rgba(15, 23, 42, 0.45);
    in-out property <color> card-border: rgba(30, 41, 59, 0.6);
    border-radius: 12px;
    background: card-bg;
    border-width: 1px;
    border-color: card-border;
}
