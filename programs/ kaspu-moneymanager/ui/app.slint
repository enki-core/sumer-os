export component AppWindow inherits Window {
    title: "Kaspu Moneymanager";
    width: 640px;
    height: 480px;
    background: #0b0f19;

    VerticalLayout {
        alignment: center;
        spacing: 12px;
        padding: 24px;

        Text {
            text: "Kaspu Moneymanager";
            font-size: 24px;
            font-weight: 700;
            color: #e2e8f0;
            horizontal-alignment: center;
        }

        Text {
            text: "Sumer OS — ready for development";
            font-size: 14px;
            color: #94a3b8;
            horizontal-alignment: center;
        }
    }
}
